import React from 'react';

const Booking = (product) => {
    console.log(product);
    return (
        <div>
            <h1>This is Booking section</h1>
        </div>
    );
};

export default Booking;